# Normalize line endings to LF for all text files (consistent across OS + Prettier/CI)
* text=auto eol=lf

# Binary files — never normalize
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
