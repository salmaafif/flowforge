// FlowForge Prisma schema.
// Data models (tenants, users, workflows, versions, runs) are added in step 3b.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
